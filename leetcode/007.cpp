#include <iostream>
#include "utils.h"
#include "leetcode.h"

using namespace std;

class Solution {
public:
    int reverse(int x) {
        int res = 0;
        while (x != 0) {
            int pop = x % 10;
            if (res res)
        }
        return res;
    }
};

int main() {
    int x = r<int>();
    Solution s;
    cout << s.reverse(x) << endl;
    return 0;
}
