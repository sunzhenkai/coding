#include <iostream>

using namespace std;

int main() {
    int a[] = {3, 2, 1, 2, 5, 2};
    int current = a[0], cnt = 0;
    for (auto i : a) {
        cout << i << endl;
        if (current == i) {
            ++cnt;
        } else if (cnt：) {

        }
    }
    return 0;
}
