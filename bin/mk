#!/bin/bash
# set -x

[ -z "$1" ] && echo "ERROR: no input file" && exit 1

if [[ "$1" =~ "cpp" ]]; then
    g++ -std=c++11 -I../template -o .mk.out "$1" && ./.mk.out
else
    g++ -std=c++11 -I../template -o .mk.out "$1.cpp" && ./.mk.out < "$1.in"
fi
